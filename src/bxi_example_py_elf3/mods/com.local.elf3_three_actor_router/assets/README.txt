Local simulation artifact:

- policy.onnx — TienKung-Lab-bxi model_33000 single-file AMP export
- policy_manifest.json — fixed ABI and checksum

This vendor handoff is UNQUALIFIED and must not be used for hardware,
ROS hardware nodes, CAN/PCIe access, or motor control.
